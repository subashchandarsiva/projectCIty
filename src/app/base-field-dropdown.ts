import { BaseFields } from './base_field';

export class DropdownQuestion extends BaseFields<string> {
  controlType = 'dropdown';
  options: {key: string, value: string}[] = [];

  constructor(options: {} = {}) {
    super(options);
    this.options = options['options'] || [];
  }
}

